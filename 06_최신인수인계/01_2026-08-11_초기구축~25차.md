# 교통비 정산서 — 초기 구축

작성일: 2026-08-11 | 프로젝트: 14_교통비정산서

---

## 작업 목표

기존 Excel 정산서를 웹 앱으로 전환. 기존 계약관리·입찰관리와 동일한 헤더 UI 패턴 적용.
유류단가 자동조회, 카카오맵 거리 자동계산, 차량 프로필 자동저장 기능 구현.

## 수행 내용

### 1. 기존 Excel 분석
- `/Users/jiseokchoi/Downloads/1111.xlsx` 바이너리 파싱 (Python zipfile + xml.etree)
- 핵심 수식 추출: `ROUND($I19 * $L$15 / IF($B$15="휘발유", IF(...), ...), -1)`
- 연비 테이블 확정:
  | 연료 | 배기량 | 연비 |
  |------|--------|------|
  | 휘발유 | 1000cc 미만 | 9 km/L |
  | 휘발유 | 1500cc 이하 | 8 km/L |
  | 휘발유 | 1500cc 초과 | 7 km/L |
  | 경유 | (무관) | 7 km/L |
  | LPG | (무관) | 5 km/L |
- 이동수단 옵션: 자차, 택시

### 2. 구현 (단일 HTML 파일)
- 경로: `/Users/jiseokchoi/Desktop/workspace/14_교통비정산서/index.html`
- SMC 로고: `/Users/jiseokchoi/Desktop/workspace/14_교통비정산서/smc-logo.png` (HASPA 프로젝트에서 복사)
- CDN: Pretendard, xlsx-js-style

### 3. 주요 기능
- **헤더 UI**: 13번 프로젝트(검사장비비교시뮬레이터)와 동일한 패턴
- **기본 정보**: 이름·차량명·번호·연료·배기량·정산기간 → LocalStorage 자동저장
- **유류단가**: 오피넷 직접 링크 + 수동 입력 → km당 단가 자동표시
  - 오피넷 CORS 이슈로 직접 fetch 불가 → 수동입력+링크 방식 채택
- **이동 내역 테이블**: 구간 추가/삭제, 이동수단(자차↔택시) 토글
  - 출발지/도착지: 카카오 REST API 장소 검색 모달
  - 거리: Kakao Mobility Directions API 우선, 실패 시 Haversine×1.3 fallback
  - 삼성의료원(127.0849986, 37.4882579) 기본 출발지
- **계산**: `Math.round(거리 × 단가 / 연비 / 10) * 10` (10원 반올림)
- **합계 카드**: 건수·총거리·유류비·정산신청액 실시간 업데이트
- **엑셀 내보내기**: xlsx-js-style로 정산서 형식 다운로드
- **인쇄**: CSS @media print 적용
- **API 키**: `995bceb54093798d160d753693a9c98e` (09번 프로젝트에서 재사용)

### 4. 검증 결과
- 유류비 계산: 5.3km × 1678원 ÷ 7 = 1,270원 ✓ (ROUND(-1) 적용)
- 단가 표시: 1678 ÷ 7 = 239.7원/km ✓
- LocalStorage 프로필 저장 동작 확인

## 산출물

| 파일 | 설명 |
|------|------|
| `index.html` | 단일 파일 웹앱 전체 |
| `smc-logo.png` | 헤더 로고 |
| `history/2026-08-11_교통비정산서-초기구축.md` | 이 문서 |

## 2차 개편 (2026-08-11)

### 버그수정 및 UI 전면 개편

**수정 항목:**
1. **장소 검색 클릭 버그** 해결: `searchResults[]` 배열 + `pickResult(i)` 패턴 (onclick 내 HTML entity 문제 완전 제거)
2. **기본 출발지** → 성균관대학교 삼성창원병원 (Kakao API 좌표 자동조회, fallback: 128.5726, 35.2286)
3. **오피넷 자동조회**: CORS 프록시(corsproxy.io → allorigins.win) fallback 체계 구현
4. **이모지 전면 제거**: 카드 타이틀·버튼 모두 SVG 아이콘으로 교체
5. **폰트** 13px → 14px 전체 확대
6. **이동내역 카드 강조**: `card-trip` 스타일 (파란 테두리 + 배경)
7. **Excel 버튼**: 계약관리시스템과 동일한 녹색(#00a661) + SVG 다운로드 아이콘
8. **엑셀 수식 포함**: `$I$3`(유류단가) · `$B$7`(연비) 참조 실제 Excel 수식 사용
9. **차량명·정산기간 필드 제거**, 작성일 기본값 = 오늘 날짜

**검증 결과 (브라우저):**
- 성균관대학교 삼성창원병원 → 창원시청 거리: 11.4km (카카오 실도로 자동계산) ✅
- 장소 클릭 선택 정상 동작 ✅
- Excel 저장 버튼 녹색 스타일 ✅

**커밋:** `77ffab0` (main → GitHub push 완료)

### Vercel 배포 상태

⚠️ Vercel 프로젝트 `smc-transport-expense`가 삭제된 상태.  
현재 보유 토큰(`vercel-smc`)에 신규 프로젝트 생성 권한 없음.

**재배포 방법 (수동):**
1. [vercel.com/dashboard](https://vercel.com) 접속
2. "Add New → Project" → GitHub `xxiohc/smc-transport-expense` 선택
3. Framework: Other, Output Directory 없음으로 설정 후 Deploy

---

## 최종 상태

**코드 완료** (GitHub main 브랜치) — **Vercel 재연결 필요**

## 3차 개편 (2026-08-11) — 로그인 + 4단계 위저드 전면 재설계

### 구현 항목 (8개 요구사항 반영)

1. **로그인 시스템** — ID + PIN (4자리) 인증, LocalStorage 저장, admin/0000 기본 계정
2. **다중 사용자** — 관리자 계정에서 사용자 추가/PIN초기화/삭제 가능
3. **나의 자동차 관리** — 사용자별 차량번호·연료·배기량 저장, 다음 로그인 시 자동 불러오기
4. **출장목적·동승자 필드 완전 제거** — 이동내역 컬럼: 월/일, 이동수단, 출발지, 도착지, 거리, 유류비, 통행료, 합계
5. **오피넷 키 교체** — F260811056 (1,500 무료 호출)
6. **정산신청 미리보기 카드 순서** — 정산신청합계(파란배경) → 이동내역 → 기본정보·유류단가
7. **codef.io 하이패스** — 브라우저에서 직접 연동 불가(서버측 OAuth+공동인증서 필요) → UI 버튼 + 안내 모달 + hipass.co.kr 링크 제공
8. **4단계 위저드 UI**
   - Step 1: 차량정보 (처음 이용 배너 → 나의 자동차 관리)
   - Step 2: 유류단가 (오피넷 자동조회)
   - Step 3: 이동내역 (구간 추가/삭제, 장소검색, 거리자동계산)
   - Step 4: 정산신청 미리보기 (인쇄·Excel·새정산서)

### 검증 (브라우저)

- admin/0000 로그인 ✅
- "처음 이용" 배너 + 차량정보 저장 ✅
- Step 1→2→3→4 전체 흐름 ✅
- 삼성창원병원 → 창원시청 11.3km 자동계산 ✅
- 유류비 2,400원 계산 (1700÷8×11.3=2,400) ✅
- 정산신청 카드 순서 정확 ✅
- 하이패스 안내 모달 ✅
- 관리자 버튼 (admin만 표시) ✅

## 4차 개편 (2026-08-11) — 스토리 기반 6단계 위저드 전면 재설계

### 사용자 요청 (새 스토리 플로우)

> 1. 자차를 이용했나요?, 택시를 이용했나요?
> 2. 자차를 이용했다고 선택 시 차량정보를 불러오시겠습니까?
> 3. 유류비 계산
> 4. 목적지 이동 계산
> 5. 하이패스 계산 or 주차비 등록
> 6. 최종 교통비정산서

### 구현 항목

1. **6단계 스토리 위저드** (자차 기준)
   - Step 1: 이동수단 선택 카드 (자차/택시, 클릭 즉시 자동 진행)
   - Step 2: 차량정보 (저장된 차량 있으면 불러오기 질문 카드 → 없으면 바로 입력폼)
   - Step 3: 유류단가 (오피넷 자동조회 + 수동 입력)
   - Step 4: 이동내역 테이블 (구간추가/거리자동계산)
   - Step 5: 하이패스/주차비 여부 카드 (있음→이동내역 입력, 없음→정산서)
   - Step 6: 최종 정산서 (파란 배경 요약 + 테이블 + 기본정보)
2. **택시 모드 조건부 단계 생략** — Step 2·3 건너뛰고 Step 1→4(기본정보 입력)→5→6
3. **09번 스타일 step trail** — 번호 점(dot) + 커넥터 + "N/총단계 · 단계명" 하단 텍스트
4. **09번 스타일 option-card** — 28px bold 질문 + 대형 카드(hover 파란 테두리)
5. **계정관리 탭** — 헤더 버튼 → PIN 변경 + 나의 자동차 편집
6. **CORS 프록시 수정** — corsproxy.io/allorigins.win → proxy.cors.sh/cors.sh
7. **컬럼명 변경** — "유류비"→"교통비(유류비)", "통행료 등"→"하이패스/주차비"
8. **하이패스 컬럼 조건부 표시** — hasToll=false 시 .toll-hidden 클래스로 열 숨김
9. **관리자 계정 체크 수정** — u.id!=='admin' → u.role!=='admin'

### 검증 (브라우저)

- 관리자/0000 로그인 ✅
- Step 1: 이동수단 선택 카드 표시 ✅
- 자차 선택 → Step 2 자동 진행 ✅
- 차량번호 입력 → Step 3 유류단가 (오피넷 1,847원/L 자동조회) ✅
- Step 4: 삼성창원병원 → 창원시청 11.4km 자동계산, 교통비 2,640원 ✅
- Step 5: 하이패스 여부 카드 ("아니요" 선택 → "최종 정산서 보기" 녹색 버튼) ✅
- Step 6: 정산서 (관리자, 2026-08-11, 1건, 11.4km, 2,640원) ✅
- 하이패스/주차비 컬럼 숨김 정상 동작 ✅

## 5차 개편 (2026-08-11) — 계약관리·입찰관리 UI 전면 통일

### 변경 항목

1. **헤더 전면 재설계** — 계약관리·입찰관리와 동일:
   - `header-inner` 래퍼 추가 (max-width: 1280px, 80px 높이)
   - 로고: 140px×40px, `object-fit: contain`
   - 서브텍스트: "SAMSUNG MEDICAL CENTER · 재무관리파트" (10px, uppercase, letter-spacing 0.04em)
   - 타이틀: "교통비 정산서" (22px, font-weight 700, letter-spacing -0.03em)
   - 헤더 우측 버튼: `icon-btn` 스타일 (transparent bg, 13px, 8px 10px padding)
   - 사용자명: `.whoami` 스팬 (13px, muted color)
   - 로그아웃: 텍스트 없이 SVG 아이콘만 표시
2. **로그인 화면** — h2 + label+input 패턴 (계약관리 동일):
   - 아이콘·h1·login-fields 구조 → h2 "로그인" + p.login-desc + label 2개
   - maxlength 4 → 8 (4~8자리 지원)
   - form-error div (display:none 초기값, JS에서 show/hide)
3. **PIN 변경 화면** — 동일 카드 패턴:
   - 기존 `.change-pin-icon`, `.cp-fields` 구조 완전 제거
   - h2 + p.cp-desc + label 2개 + form-error div
4. **CSS 정리**:
   - 중복 `/* ── Change PIN Screen ── */` 블록 삭제
   - `wizard-bar top: 60px → 80px` (헤더 높이 보정)
5. **Windows 폰트** — 기존 `@media (max-resolution: 1dppx)` 맑은 고딕 유지
6. **JS 오류 표시** — `showErr(el, msg)` 헬퍼 함수 추가 (style.display 연동)
7. **버그수정** — 오피넷 LPG 코드 K015, 관리자 삭제 버튼 role 체크

### 커밋

`042228f` — GitHub push → Vercel 자동배포

## 6차 개편 (2026-08-12) — 이동내역 컬럼 정리

### 변경 항목

1. **이동수단 컬럼 제거** — step1에서 이미 선택하므로 step4 테이블에서 삭제
2. **하이패스/주차비 컬럼 제거** — step5 총액 직접 입력 방식으로 대체
3. **택시 실비 입력** — 이동수단 컬럼 대신 교통비 셀에 taxi amount input 표시

### 커밋

`957fde4` — GitHub push → Vercel 자동배포

---

## 7차 개편 (2026-08-12) — 하이패스 이동내역 복귀 + UX 개선

### 변경 항목

1. **하이패스 컬럼 이동내역 복귀**
   - step4 테이블에 `하이패스` 컬럼 추가 (per-row 입력, 자차·택시 모두)
   - `trips.toll` 필드 복원, `calcTotal()` = 교통비 + 하이패스
   - 테이블 컬럼 순서: 월/일 | 출발지 | 도착지 | 거리(km) | 교통비(유류비) | 하이패스 | 합계

2. **Step 5: 주차비로 변경**
   - 질문: "주차비가 발생했나요?" (하이패스는 이동내역에서 입력 안내)
   - 스텝명: 통행료 → 주차비 (trail, 버튼 텍스트 모두 반영)

3. **저장 차량 불러오기 개선**
   - `useSavedCar()`: carInputForm 표시 없이 step3 직행
   - 유종(B027/D047/K015)을 저장 차량 fuel에서 자동 매핑 → fuelTypeSelect 자동 선택 + fetchOpinet() 자동 호출

4. **장소 검색 자동완성**
   - `oninput` + 400ms debounce → 2자 이상 입력 시 Enter 없이 자동 검색
   - `onSearchInput()` 함수 추가

5. **km당 단가 계산 기준 표시**
   - step3 fuel-per-km 영역에 `perKmFormula` div 추가
   - 표시 형식: `1,864원/L ÷ 7km/L = 266원/km`
   - step6 pvInfo km당 단가에도 소계(원/L ÷ km/L) 힌트 표시

6. **Excel 7컬럼 업데이트**
   - 하이패스 컬럼(F) 추가, 합계(G)로 이동
   - 차량정보 참조 H/I 컬럼으로 변경 ($I$3/$I$5)
   - 합계 수식: `=E${row}+F${row}`

7. **step6 preview 업데이트**
   - pvTable 하이패스 컬럼 추가
   - pvFoot: 하이패스 행 합계 + 주차비 별도 행 + 최종 합계

### 검증 (브라우저)

- 자차 선택 → step2 저장 차량(52두6356·휘발유·1500cc초과) 표시 ✅
- "네, 불러옵니다" → step3 직행, fuelTypeSelect 휘발유 자동 선택 ✅
- 오피넷 자동조회 1,864원/L, km당 단가 266원/km 수식 표시 ✅
- step4 이동내역: 하이패스 컬럼 표시 ✅
- step5: "주차비가 발생했나요?" 질문, trail "주차비" 텍스트 ✅
- 장소 검색 자동완성: "창원시청" 타이핑 → 검색 버튼 없이 결과 표시 ✅

### 커밋

`e3e91d4` — GitHub push → Vercel 자동배포

---

## 8차 개편 (2026-08-12) — 모바일 반응형 + 사용자 관리 재설계 + 네비게이션 통일

### 변경 항목

1. **모바일 반응형** (`@media (max-width: 640px)`)
   - 헤더: 52px 높이, 로고 76px, whoami·관리자 버튼 숨김, title white-space: nowrap
   - 위저드바: step-trail 숨김 → trail-current-info 카드형 표시 (46px, 좌측 back 버튼)
   - trip 테이블: min-width 해제, thead 숨김, `<tr>` → 2열 grid 카드형 (data-label 위쪽)
   - 각종 form-row: 1열 컬럼, search-modal 하단 시트형 (border-radius 20px 20px 0 0)
2. **네비게이션 바 교육출장 앱과 통일**
   - 헤더 우측 `header-progress-text` "N / M 단계" 표시 추가
   - `back-btn` 뒤로 버튼 (왼쪽 chevron SVG, step 1에서 hidden)
   - `trail-current-info` 활성 dot 아래 가운데 정렬
3. **화면 너비 확장**: 860→960px (`wizard-content`), 1080→1280px (`#step4`)
4. **사용자 관리 성명+부서 체계**
   - 성명·부서·PIN 입력으로 신규 사용자 추가 (ID는 자동생성 `'u'+Date.now()`)
   - 로그인: name으로 검색 (기존 id 하위호환 유지)
   - 관리자 패널 테이블: 성명/부서/권한/차량번호/연료/배기량 + 차량수정/PIN초기화/삭제
   - 계정관리 패널: 성명·부서 표시
5. **숫자 천 단위 실시간 콤마** — `applyComma(el)` 헬퍼, 모든 숫자 input에 oninput 적용
6. **하이패스 실시간 합계** — toll/amount oninput에서 직접 trips 업데이트 + recalcAll()

### 커밋

`600f643` — GitHub push → Vercel 자동배포

---

## 9차 개편 (2026-08-12) — 9차 이후 통합

> 8차에 포함하여 단일 커밋으로 처리됨

---

## 10차 개편 (2026-08-12) — 하이패스 실시간 합계·기본정보 4열·인쇄 페이지분할

### 변경 항목

1. **하이패스/택시 금액 실시간 합계**
   - `onchange` → `oninput` 통합: `applyComma(this);trips.find(...).toll=...;recalcAll()`
   - 커서 이동 없이 타이핑 중에도 합계 즉시 업데이트

2. **기본정보 섹션 4열 컴팩트 레이아웃**
   - `.info-grid-4` 클래스 추가: `grid-template-columns: repeat(4, 1fr)`
   - 라벨 11px, 값 14px (기존보다 작게)
   - pvInfo div에 `info-grid-4` 클래스 적용 → 2행×4열 (신청자·작성일·차량번호·연료구분 / 배기량·연비·유류단가·km당단가)

3. **20줄+ 이동내역 인쇄 페이지 분할**
   - `.trip-card` 클래스를 이동내역 카드에 추가
   - `@media print`: `.trip-card { page-break-inside: auto !important }`
   - `thead { display: table-header-group }` → 페이지마다 헤더 반복
   - `tfoot { display: table-footer-group }` → 마지막 페이지에 합계
   - `tbody tr { page-break-inside: avoid }` → 행 분리 방지
   - 인쇄 시에도 info-grid-4 4열 유지

### 검증

- 25행 이동내역 테이블 전체 렌더링 ✅ (1452.5km, 416,770원)
- 기본정보 2행×4열 레이아웃 확인 ✅
- 하이패스 실시간 합계 구현 확인 ✅

### 커밋

`43e6c32` — GitHub push → Vercel 자동배포 진행 중

---

## 11차 개편 (2026-08-12) — 테스트 사용자 주입 + 임시저장 기능

### 사용자 요청

1. 계약관리시스템에 등록된 실제 직원 + 다양한 유종/배기량 차량을 교통비정산서에 등록
2. 작성 중단 후 재개할 수 있는 임시저장 기능

### 구현 항목

#### 1. 테스트 사용자 주입 (16명)

Supabase `contract-db` 버킷에서 실제 직원 명단을 조회하여 `te_users` 주입.
차량 유종/배기량을 다양하게 분산 배정:

| 연료 | 배기량 | 대표 사용자 |
|------|--------|------------|
| 휘발유 | 1000cc 미만 | 이준경, 박수현 |
| 휘발유 | 1500cc 이하 | 박미숙, 조윤서 |
| 휘발유 | 1500cc 초과 | 김도영, 나정민, 구연정 |
| 경유 | 1500cc 이하 | 심규옥, 김종국, 정미선, 장성웅 |
| LPG | 1500cc 이하 | 손정균, 권민, 권기범, 윤종부 |

연비 계산 검증:
- 휘발유 1000cc 미만: 9 km/L ✅
- 휘발유 1500cc 이하: 8 km/L ✅
- 휘발유 1500cc 초과: 7 km/L ✅
- 경유: 7 km/L ✅
- LPG: 5 km/L ✅

#### 2. 임시저장 기능

**저장 키**: `te_draft_{userId}` (사용자별 분리, 7일 만료)

**저장 항목**: tripMode, currentStep, trips, tripIdSeq, hasToll, totalTollAmount, 폼 입력값 전체

**임시저장 버튼**: Step 2·3·4·5 하단 step-actions 중앙에 노란 버튼 추가
- 저장 시 2초간 초록 "저장됨" 상태로 시각 피드백

**임시저장 배너**: 로그인 후 `checkDraft()` 실행
- 저장본이 있으면 헤더 직하 오렌지 배너 표시
- "이어서 작성하기" (오렌지 버튼) → `restoreDraft()` 실행
- "새로 시작" (회색 버튼) → `discardDraft()` 후 Step 1

**draft restore 동작**:
- tripMode 상태 복원 (자차/택시 UI 분기)
- 폼 필드 값 복원 (차량번호·연료·배기량·유류단가 등)
- 이동내역 trips 배열 복원 → `renderTrips()` 재실행
- 하이패스/주차비 상태 복원
- 저장 당시 단계로 `gotoStep()` 이동

**z-index 스택**: `draft-banner`는 sticky 없음(fixed header 직하에 흐름 배치),
`wizard-bar`는 배너 높이만큼 `style.top` 동적 조정 (80 + bh px)

**resetWizard()** → `clearDraft()` 호출로 기존 임시저장 삭제

### 검증

- 자차 선택 → Step 2 진행 → 임시저장 버튼 클릭 → 저장됨 피드백 ✅
- 새 정산서 → 로그인 화면 → 재로그인 → 오렌지 배너 표시 ✅
- "이어서 작성하기" → Step 2 복원, 폼 값(차량번호·연료·배기량) 정상 복원 ✅
- wizard-bar top 동적 조정으로 배너 가려짐 없음 ✅
- 7일 만료 로직 확인 (Date.now() - savedAt > 7일) ✅

### 커밋

11차 개편 커밋 예정

---

## 최종 상태 (11차 개편)

**코드 완료** — main 브랜치 push 예정

---

## 12차 개편 (2026-08-12) — 사용자관리 계약관리시스템 동일화 + 택시 모드 전면 개선

> 커밋 `ec1119c` — GitHub push 완료

### 주요 변경
1. **사용자 관리 UI** — 계약관리시스템(11번)과 동일한 패턴으로 통일
2. **택시 모드 전면 개선** — Step 1→4→6 흐름, 이동내역 택시행 별도 렌더링
3. **이동내역 모바일 카드**: `<tr class="taxi-row">` / `<tr class="car-row">` 클래스 추가 (CSS nth-child 선택자 활용)

---

## 13차 개편 (2026-08-12) — 모바일 UI 전면 감사 및 수정

### 작업 목표
375×812 모바일 환경에서 모든 화면 점검 → 레이아웃 깨짐·단위 표시·버튼 잘림·행 높이 이상 전수 수정.
동시에 사용자 요청 4가지 기능 추가.

### UI 감사 수정 내역

| 위치 | 증상 | 수정 |
|------|------|------|
| Step 1 option-card | 설명 텍스트 음절 중간 줄바꿈 | `word-break: keep-all` |
| Step 2 차량 미리보기 | 차량번호 텍스트 overflow | `text-overflow: ellipsis` + `min-width: 0` |
| Step 3 하단 버튼 | "임시저\n장", "다음: 유류단가 확\n인" 잘림 | `white-space: nowrap; flex-shrink: 0` on btn |
| Step 3 "원/km" | 숫자 아래 단위 배치 | flex row `align-items: baseline; gap: 4px` |
| Step 4 삭제 버튼 | 카드 하단에 뚝 떨어짐 | `grid-row: 1` → date 옆 배치 |
| Step 4 하단 버튼 | 잘림 | nowrap + flex-shrink:0 |
| Step 6 요약 단위 | 숫자 아래 "건"·"km"·"원" | `.val-row { display:flex; align-items:baseline; gap:4px }` |
| Step 6 기본정보 | 4열이 너무 좁음 | `@media (max-width:640px)` → 2열 |
| Step 6 미리보기 테이블 | 셀 줄바꿈 | `white-space: nowrap` |
| Step 6 타이틀 | "정산신청" 고정 | `id="pvSummaryTitle"` + JS에서 자차/택시 동적 갱신 |
| 관리자 패널 테이블 | 텍스트 글자 단위 줄바꿈 | `overflow-x: auto` wrapper + `white-space: nowrap` |
| 관리자 패널 행 높이 | 각 행 124px (버튼 3줄 wrap) | `flex-wrap: nowrap` on 작업 td |
| 관리자 패널 padding | 좌우 여백 과다 | `padding: 20px 12px` mobile override |

### 기능 추가

1. **단위 인라인 표시** — 요약 카드 숫자+단위를 `flex row`로 나란히 표시 ("1 건", "11.4 km", "2,640 원")
2. **로고 → 홈 이동** — `.header-brand` onclick → `goHome()` 호출 (임시저장 후 Step 1 복귀, confirm 다이얼로그)
3. **정산신청 타이틀 동적 표시** — `buildPreview()` 진입 시 tripMode에 따라 "교통비 정산신청 (자차)" / "교통비 정산신청 (택시)" 표시
4. **계정관리 패널** — 모바일 확인 완료 (레이아웃 정상)

### 산출물
- `index.html` — 73줄 추가, 26줄 삭제 (net +47줄)

### 다음 단계

- [ ] **크로스 디바이스 동기화** (HIGH): 현재 LocalStorage → Supabase 마이그레이션 필요. PC에서 관리자가 저장한 차량 정보가 모바일에서 조회 가능해야 함. Supabase 프로젝트 URL + anon key 필요 (사용자에게 문의)
- [ ] Vercel 배포 (GitHub push 후 자동배포)
- [ ] 인쇄 미리보기 페이지 분할 확인

---

## 14차 개편 (2026-08-12) — 모바일 카드 전면 재설계 + UX 개선

커밋: `9a4cbf9`

### 작업 목표

iPhone 16 Pro 실기기 스크린샷에서 발견된 모바일 UI 문제 3건 + 사용자 요청 2건 반영.

### 수행 내용

#### 모바일 트립 카드 전면 재설계
- 기존: `grid-template-columns: 1fr 1fr` → 거리/교통비, 하이패스/합계가 2×2 배치
- 변경: `grid-template-columns: repeat(4, 1fr)` → **4가지 항목이 한 줄**
- 구조: 날짜(col1-3)+삭제(col4) 헤더 row / 출발지(full) / 도착지(full) / 거리·교통비·하이패스·합계 4열 row
- 자차(car-row): td:8=삭제 col4 row1, td:4-7=스탯 col1-4 row4
- 택시(taxi-row): td:7=삭제 col4 row1, td:4=거리 col1, td:5=금액 col2-3, td:6=합계 col4
- `overflow:hidden` + `border-radius:14px` 카드 테두리 정리

#### 출발지/도착지 좌측정렬
- `.trip-place-btn { max-width: 100% !important; width: 100%; text-align: left; }` 모바일 오버라이드

#### 검색 모달 CSS 버그 수정
- 기존 `.search-modal { ... }` → HTML 요소는 `.modal-box` 클래스라 스타일 미적용 (하단 시트 깨짐)
- 수정: `#modalSearch .modal-box { ... }` + `overflow-y: auto` 추가

#### 추정 배지 제거 (사용자 요청)
- "추정 거리도 충분히 정확하니 배지 불필요"
- `dist-badge.est` CSS 제거, JS `distBadge` 로직에서 est 분기 제거
- est 모드 직접입력 오버라이드 `<input>` 제거
- `buildPreview()` 추정값 경고 항목 제거

#### Step 2 차량 불러오기 UI 재설계 (사용자 요청)
- 기존: "네, 불러옵니다" + "아니요, 새로 입력" 두 option-card 가 동일 스타일로 나란히 배치 → 구분 어려움
- 변경: **차량 카드 전체 = 불러오기 CTA** (`.car-load-btn`, blue border), "이 차량 정보로 계속하기 →"
- "또는" 구분선 (`.or-divider` with `::before`/`::after` hr-lines)
- "새로 작성하기" → 텍스트 링크 (`.new-car-link`) 로 분리 → 명확한 primary/secondary 계층

#### 신규 사용자 차량 등록 유도 (사용자 요청)
- PIN 변경 후 `currentUser.car?.plate` 없으면:
  - `startApp()` 실행 후 350ms 딜레이 → `openAccount()` 자동 호출
  - 토스트: "환영합니다! 차량 정보를 먼저 등록해 주세요."
  - 차량번호 입력 필드로 스크롤 + focus
- 계정관리 패널 "나의 자동차" 카드 상단에 `#carRegNotice` 안내 배너 (차량 없을 때만 표시)

### 검증

| 기기 | 해상도 | 결과 |
|------|--------|------|
| Samsung Galaxy | 360×800 | ✅ 4열 정상 |
| iPhone 14 | 375×812 | ✅ 4열 정상 |
| iPhone 16 Pro Max | 430×932 | ✅ 4열 정상 |
| Step 2 (저장 차량 있음) | 375px | ✅ 카드+CTA+구분선+링크 |

### 다음 단계
- [ ] **크로스 디바이스 동기화** (HIGH): LocalStorage → Supabase
- [ ] 인쇄 미리보기 페이지 분할 확인

---

## 15차 개편 (2026-08-12) — 모바일 전기종 레이아웃 정밀 수정 (커밋 0fbdd2c)

### 사용자 요청

> "닌 직접 장소입력 등 전부 처음 로그인 단계부터 끝까지 하면서 모바일로 화면 깨지는부분 각 단계별 페이지가 줌인, 줌아웃되지않는지 하나하나 기종별, 크기별로 다 체크해서 이상한부분수정해"

### 수행 내용

- **PIN 플레이스홀더 깨짐 수정**: `input[type="password"]`에 `letter-spacing: 0.2em` 인라인 스타일이 placeholder에도 적용되어 "현 재 P I N" 처럼 spacing이 벌어짐 → `input[type="password"]::placeholder { letter-spacing: 0; text-align: left; }` CSS 추가
- **Step 6 하단 버튼 360px 오버플로우**: 4개 버튼(수정/인쇄/Excel/새 정산서)이 2px 초과 → `.btn-sm` padding `7px 12px` → `6px 9px`, gap 5→3px, font-size 13→12px; `#step6 .step-actions > div` gap 10→6px
- **전기종 검증**: 360/375/390/414/430px — 모든 단계(1~6) 줌인/아웃·레이아웃 깨짐 없음 확인

---

## 16차 개편 (2026-08-12) — 차량 등록 전용 화면 추가 (커밋 8f79f1f)

### 사용자 요청

> "PIN을 변경하고 그뒤에 자동차 등록이 한화면에 표현되야한다 (PIN변경화면 넘어간 후 자동차 등록화면 필요)"
> "자동차등록이 안된상태에서 로그아웃 했는데 PIN치니까 자동차등록화면이 안나온다"

### 수행 내용

#### 새 `#carRegScreen` 전용 오버레이 추가

- 기존: PIN 변경 후 `startApp()` → `openAccount()` 자동호출 (계정관리 패널 슬라이드인)로 차량 등록 유도 → 별도 화면이 없어 어색
- 변경: `#carRegScreen` fullscreen fixed 오버레이 (loginScreen·changePinScreen과 동일 패턴, z-index 1000)
- 카드 내 구성: 차량 아이콘, "차량 정보 등록" h2, 설명 텍스트, 차량번호 input, 연료/배기량 select (2열 grid), "차량 등록 후 시작하기" 버튼, "나중에 등록하기" 텍스트 버튼

#### 라우팅 수정

- `doLogin()`: PIN이 '0000' 이면 PIN변경 화면으로. 일반 로그인 후 `user.car?.plate` 없으면 `showCarReg()` 호출, 있으면 `startApp()`
- `doChangePin()`: PIN 변경 완료 후 `currentUser.car?.plate` 없으면 `showCarReg()`, 있으면 `startApp()`

#### 신규 함수

- `showCarReg()`: carRegScreen 표시, 필드 초기화, cr_plate focus
- `crFuelChange()`: 경유/LPG 선택 시 배기량 라벨 숨김
- `doCarReg()`: 차량번호 검증 → `updateUser()` → `startApp()` + 토스트
- `doSkipCarReg()`: carRegScreen 닫고 `startApp()`

---

## 17차 개편 (2026-08-13) — UI 정제 4건 (커밋 ca8d93f)

### 사용자 요청

1. "지금 차량 픽토그램이 트럭같은건데 귀여운 뉴비틀 같은 느낌으로 바꿔줘" → "전형적인 자동차로 그냥해"
2. "마지막 교통비 정산서에 부서이름도 나오게하고"
3. "소제목 옆 작은 픽토그램은 뭔가 별로다. 좀 더 심플하지만 소제목을 잘구분지어주는 단순한걸로 교체해줘"
4. "그리고 경유랑 LPG는 cc선택이 없다"

### 수행 내용

- **차량 픽토그램 교체**: 트럭/밴 SVG → 전형적인 세단 실루엣 (cabin 삼각 dome + 직사각 body + 바퀴 2개). 4곳 전체 `replace_all` 일괄 교체
- **소제목 아이콘 단순화**: `.card-title-icon` CSS 변경만으로 처리 — `32px×32px` 파란 배경 박스+SVG → `3px×20px` 파란 좌측 바 (SVG는 `display:none`). HTML 수정 없이 CSS만으로 전체 적용
- **정산서 부서 표시**: step6 `summary-top`에 `#pvDept` 추가 (이름 바로 아래), `renderStep6()` 내 `currentUser.dept` 바인딩. `pvInfo`(기본정보 섹션)에도 "부서" 항목 추가 — 자차/택시 모드 양쪽 모두
- **경유/LPG 배기량 숨김**: `updateEfficiency()` 내 `dispCol.style.opacity = 0.4` → `dispCol.style.display = 'none'/'""'` 변경 (완전 숨김)

### 검증

| 항목 | 결과 |
|------|------|
| 세단 아이콘 (SVG innerHTML) | ✅ sedan 경로 적용 |
| 경유 선택 → dispCol | ✅ display:none |
| step6 `pvDept` 텍스트 | ✅ "재무관리파트" |
| pvInfo 부서 항목 | ✅ innerHTML.includes('부서') true |
| 소제목 좌측 바 | ✅ 스크린샷 확인 |

### 현재 상태

- Vercel 배포: https://smc-transport-expense.vercel.app (커밋 ca8d93f)
- 완료 상태, 이슈 없음

### 다음 단계

- [x] **크로스 디바이스 동기화** → 19차에서 완료

---

## 18차 수정 (2026-08-13) — LPG/경유 차량 카드 cc 표시 제거 (커밋 a9868b3)

### 사용자 요청

> (step2 화면 스크린샷) "이 화면에서 여전히 LPG인데 cc가 나오고있다"

### 수행 내용

- `savedCarText` (step2 내 저장 차량 불러오기 버튼 텍스트): `${car.displacement}` 무조건 표시 → `${car.fuel === '휘발유' ? ' · ' + car.displacement : ''}` 조건부로 변경
- 경유/LPG 차량 카드에서 cc 정보가 완전히 제거됨

### 커밋

`a9868b3` — GitHub push → Vercel 자동배포

---

## 19차 개편 (2026-08-13) — 크로스 디바이스 동기화 (GitHub DB + JWT 백엔드) (커밋 330805b)

### 사용자 요청

> "지금 휴대폰, pc, 노트북 각각 다른 곳에서 같은 계정으로 접속하니 계속 초기화되는것같다"
> "응 본인 계정으로 어디든 접속하고, 그 정보 데이터들이 누적 연동 저장되어야지"

### 아키텍처 전환

| 항목 | 이전 (18차까지) | 이후 (19차~) |
|------|--------------|-------------|
| 사용자 저장 | `localStorage.te_users` (디바이스별) | GitHub `db` 브랜치 `db.json` (공통) |
| 인증 세션 | `localStorage.te_session` | `sessionStorage.te_token` (JWT 8hr) |
| PIN 검증 | 평문 비교 `user.pin === pin` | SHA-256 해시 비교 |
| 서버 | 없음 (정적 HTML) | Node.js HTTP 서버 + Vercel 서버리스 |
| 파일 구조 | `index.html` (루트) | `public/index.html`, `server.js`, `api/index.js`, `package.json` |

### 수행 내용

#### 신규 파일

- `server.js`: Node.js HTTP 서버 (ESM), GitHub DB 읽기/쓰기, JWT 생성/검증, SHA-256 PIN 해싱
  - Salt: `"smc-transport-expense-2026"`
  - JWT TTL: 8시간
  - GH_CACHE_MS: 10초 (중복 요청 방지)
  - Routes: `POST /api/login`, `POST /api/initial-pin`, `POST /api/change-pin`, `GET /api/me`, `PUT /api/me/car`, `GET /api/users`, `POST /api/users`, `PUT /api/users/:id`, `POST /api/users/:id/reset-pin`, `DELETE /api/users/:id`
  - 정적 파일 서빙: `public/` 디렉터리
  - 직접 실행 시에만 `createServer().listen()` 호출 (Vercel 서버리스 호환)
- `api/index.js`: `export default handle` (Vercel 서버리스 엔트리)
- `package.json`: `"type": "module"`, `"engines": {"node": ">=18"}`

#### `vercel.json` 수정

- 기존: `rewrites` (정적 HTML 서빙)
- 변경: `functions.api/index.js.maxDuration = 30`, `routes` 전부 `api/index.js`로 라우팅

#### `public/index.html` 수정

- `KEY_USERS`, `KEY_SESSION`, `loadUsers()`, `saveUsers()`, `getUser()`, `updateUser()` 전부 제거
- `TOKEN_KEY = 'te_token'`, `async api(path, opts)` 헬퍼 추가
- async 전환된 함수: `doLogin()`, `doChangePin()`, `doCarReg()`, `step2Next()`, `autoLogin()`, `renderUserTable()`, `saveEditCar()`, `addUser()`, `resetPin()`, `deleteUser()`, `changePin()`, `saveAccountCar()`
- 임시저장(draft)은 계속 LocalStorage 유지 (디바이스 내 작업 중간 상태)

#### GitHub `db` 브랜치 초기화

- `db.json` 최초 등록: admin 계정 (최지석, 재무관리파트, PIN=1234)
- PIN hash: `SHA-256("1234" + "smc-transport-expense-2026")` = `28abc438...`

#### Vercel 환경 변수

- `GITHUB_DB_TOKEN`: GitHub PAT (repo 권한)
- `GITHUB_DB_REPO`: `xxiohc/smc-transport-expense`
- `JWT_SECRET`: 랜덤 hex 64자

### 검증

| 항목 | 결과 |
|------|------|
| `POST /api/login` (최지석/1234) | ✅ JWT 토큰 반환 |
| 브라우저 로그인 | ✅ 메인 앱 진입 |
| 관리자 패널 사용자 목록 | ✅ GitHub db.json 최지석 표시 |
| 크로스 디바이스 | ✅ sessionStorage JWT → 어느 기기든 동일 db 공유 |

### 커밋

`f569413` (19차 초기), `330805b` (handle export 수정) — GitHub push → Vercel 자동배포

### 현재 상태 (2026-08-13, 25차)

- Vercel 배포: https://smc-transport-expense.vercel.app (커밋 97af791)
- 운영 중, 이슈 없음

---

## 20차 (2026-08-13) — 관리자 패널 부서별 그룹화 + 유류단가 카드 UI

커밋: `68ab196`

- 사용자 관리 테이블: 부서별 그룹 헤더 삽입 (계약시스템과 동일 방식)
- 유류단가 카드: grid 2열 레이아웃 (리터당 단가 ↔ km당 단가 분리)
- km당 단가 38px 강조 표시, 유류단가 입력 44px

---

## 21차 (2026-08-13) — 정산서 기본정보 중복 항목 제거

커밋: `4b7f00c`

- 정산서 기본정보에서 작성일·연비·유류단가 제거 (km당 단가에 계산식 이미 포함)
- 섹션 타이틀 '기본정보 및 유류단가' → '기본정보'

---

## 22차 (2026-08-13) — 기본정보 6열 1행 + iOS 스크롤 + 수식 간소화

커밋: `2567961`

- 정산서 기본정보 grid: 4열2행 → 6열1행 (데스크탑), 모바일 3열2행
- 유류단가 카드 수식: `= 266원/km` 결과값 중복 제거 → `1,864원/L ÷ 7km/L` 만 표시
- 모바일(≤640px): `#perKmFormula { display:none }` — 수식 숨김, km단가 48px
- iOS Safari scrollTo 대응: `document.documentElement.scrollTop`, `document.body.scrollTop` 추가

---

## 23차 (2026-08-13) — iOS 스크롤 rAF 픽스 + PDF 버튼 레이블 + print CSS

커밋: `5cbfaaa` → `ea5a43a`

- `gotoStep()`: `requestAnimationFrame` 안에서 scrollTo 실행 (iOS Safari DOM 변경 후 scrollTo 무시 버그 수정)
- 인쇄 버튼 레이블 '인쇄' → 'PDF' (모바일에서 인쇄=PDF 저장)
- print CSS: `info-grid-6` 추가 (인쇄 시 6열 1행 유지)
- 단계 이동 시 제일 위에서 화면 시작 확인

---

## 24차 (2026-08-13) — 메일 전송 기능 제거

커밋: `ea5a43a`

- 23차에서 추가했던 '메일 전송' 버튼 및 `sendSettlementEmail()` 함수 삭제

---

## 25차 (2026-08-13) — 관리자 검색/필터 + XSS 수정 + 서버 보안 강화

커밋: `97af791`

- **관리자 사용자 검색**: 이름·부서 텍스트 검색, PIN 상태 필터(전체/미변경/변경됨), 결과 카운트 표시
- **캐시 분리**: `loadUsers()`(API 호출) / `renderUserTable()`(캐시 필터링, 동기) 분리
- **PIN 배지**: 테이블에 미변경(노란)/변경됨(초록) 배지 표시
- **XSS 수정**: buildPreview `pvBody`, `pvInfo` innerHTML — 이름/부서/출발지/도착지/차량번호 모두 `escapeHtml()` 적용
- **JWT_SECRET**: 환경변수 미설정 시 console.warn (fallback은 유지, 로컬 개발 대응)
- **Rate Limiting**: 로그인 5회 실패 시 15분 잠금 (로컬 서버에서 유효, Vercel 서버리스는 부분 동작)
- **writeDb 에러 전파**: GitHub PUT 실패 시 throw → handle() catch → 클라이언트에 500 반환 (기존 silent fail 수정)

---

## db.json 계정 정비 (2026-08-13)

- 관리자(admin) + 최지석(user) + 재무관리파트 등 34명 = 총 35계정
- 최지석·관리자 제외 32명 차량정보 초기화 (car: null)
- 전원 초기 PIN: 0000 (첫 로그인 시 변경 필수)

---

## 폴더 정리 (2026-08-13)

삭제된 파일:
- `index.html` (루트, 구버전 — public/index.html이 현재 버전)
- `smc-logo.png` (루트, public/smc-logo.png 중복)
- `스크린샷 2026-08-11 오후 4.41.26.png`

현재 폴더 구조:
```
14_교통비정산서/
├── api/index.js          # Vercel 어댑터
├── public/
│   ├── index.html        # 메인 앱 (단일 파일)
│   └── smc-logo.png
├── history/
│   └── 2026-08-11_교통비정산서-초기구축.md
├── server.js             # Node.js HTTP 서버 (GitHub DB + JWT)
├── package.json
├── vercel.json
└── .gitignore

GitHub db 브랜치: db.json (계정 DB)
```

### 다음 단계

- 필요 시 직원 계정 차량정보 등록 (관리자 패널에서 개별 입력)
- 인쇄/PDF 최종 레이아웃 확인
